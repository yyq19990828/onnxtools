# Config

全局常量与 YAML 配置加载工具。`BaseORT` / `InferencePipeline` / `OcrORT` 在缺省路径下都会回退到这里。

## 配置加载

::: onnxtools.config.load_det_config
    options:
      heading_level: 3

::: onnxtools.config.load_plate_config
    options:
      heading_level: 3

::: onnxtools.config.load_visualization_config
    options:
      heading_level: 3

::: onnxtools.config.get_ocr_character_list
    options:
      heading_level: 3
