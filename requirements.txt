# ============================================
# 核心依赖 (必需)
# ============================================
onnxruntime-gpu[cuda,cudnn]==1.22.0
opencv-contrib-python>=4.12.0.88
numpy>=2.2.6
pyyaml>=6.0.2
colorlog>=6.9.0
supervision==0.26.1
pytest>=7.0.0
pytest-cov>=7.0.0
pytest-benchmark
Pillow>=11.3.0
onnx==1.17.0
onnx-graphsurgeon
onnxslim>=0.1.65
polygraphy>=0.49.26
python-levenshtein>=0.25.0
matplotlib>=3.10.6
colored>=2.3.1

# ============================================
# 可选依赖: TensorRT加速支持
# ============================================
# 如需TensorRT加速，请按以下步骤安装:
#
# 方法1: 使用uv安装 (推荐)
# 1. 先安装构建依赖:
#    uv pip install pip setuptools wheel
# 2. 使用可选依赖组安装:
#    uv pip install -e ".[trt]"
#
# 方法2: 手动安装TensorRT包
#    uv pip install tensorrt==8.6.1.post1 tensorrt-bindings==8.6.1 tensorrt-libs==8.6.1 \
#      --extra-index-url https://pypi.nvidia.com
#
# 注意: TensorRT安装需要禁用构建隔离 (已在pyproject.toml中配置)
# ============================================
